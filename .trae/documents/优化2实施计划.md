# 优化2实施计划

## 一、优化2.md分析总结

### 1.1 文档来源
优化2.md是一份针对基金投资脚本项目的深度优化建议文档，涵盖技术、投资、用户体验、安全、合规、扩展等7个维度。

### 1.2 代码库现状分析

| 模块 | 现状 | 需改进 |
|------|------|--------|
| fund_database.py | 参数化查询（安全）、有单列索引 | 缺复合索引、无清理功能、无journal表 |
| fund_data_fetcher_v2.py | 多数据源降级、重试机制 | 缺数据完整性校验、无请求缓存 |
| fund_analyzer_v2.py | RSI/Bollinger/MACD已向量化 | 缺基准对比、免责声明 |
| fund_notifier.py | 支持微信/邮件/控制台 | 凭证硬编码在config.yaml |
| config_manager.py | YAML配置、CLI支持 | 缺环境变量读取 |
| main_cli.py | analyze/monitor/backtest/simulate/list/config | 缺wizard/journal/review/compare |
| config.yaml | 完整配置模板 | 缺安全提示注释 |
| README.md | 基础使用说明 | 缺免责声明、新功能文档 |

### 1.3 无需优化项（已满足）

| 项目 | 原因 |
|------|------|
| 技术指标向量化 | RSI用`rolling().mean()`、Bollinger用`rolling().mean()/std()`、MACD用`ewm().mean()`，已是向量化实现 |
| SQL注入防护 | 所有INSERT/SELECT均使用`?`参数化占位符，`get_fund_summary()`为静态查询无用户输入 |
| 数据库基础索引 | 已有`idx_nav_fund_code`、`idx_nav_date`、`idx_invest_fund_code`、`idx_analysis_fund_code` |

### 1.4 需要新增的依赖库

```
requests-cache    # HTTP请求缓存
plotly            # 交互式HTML图表
jinja2            # HTML报表模板
watchdog          # 配置文件热加载
```

---

## 二、实施计划

### 第一阶段：安全与合规（优先级：高）

#### 步骤1：添加免责声明

**修改文件**：
- `scripts/README.md` - 顶部添加免责声明
- `scripts/fund_data_fetcher.py` - 脚本顶部docstring
- `scripts/fund_data_fetcher_v2.py` - 脚本顶部docstring
- `scripts/fund_analyzer.py` - 脚本顶部docstring
- `scripts/fund_analyzer_v2.py` - 脚本顶部docstring
- `scripts/fund_database.py` - 脚本顶部docstring
- `scripts/fund_notifier.py` - 脚本顶部docstring
- `scripts/main.py` - 脚本顶部docstring
- `scripts/main_cli.py` - 脚本顶部docstring
- `scripts/config_manager.py` - 脚本顶部docstring
- `scripts/demo_analysis.py` - 脚本顶部docstring

**免责声明内容**：
```
免责声明：
本工具仅供个人学习和研究使用，所展示的数据均来自第三方公开接口。
不构成任何投资建议，据此操作风险自担。
基金投资有风险，过往业绩不代表未来表现。
```

#### 步骤2：凭证安全改造

**修改文件**：`scripts/config_manager.py`

在ConfigManager类中添加`get_secure()`方法：

```python
def get_secure(self, key_path, env_var=None, default=None):
    if env_var:
        env_value = os.environ.get(env_var)
        if env_value:
            return env_value
    return self.get(key_path, default)
```

添加敏感配置的环境变量映射：

```python
SECURE_CONFIG_MAP = {
    'notification.wechat.webhook_url': 'WECHAT_WEBHOOK',
    'notification.email.sender_email': 'EMAIL_SENDER',
    'notification.email.sender_password': 'EMAIL_PASSWORD',
    'notification.email.receiver_email': 'EMAIL_RECEIVER',
}
```

**修改文件**：`scripts/fund_notifier.py`

在`send_wechat()`和`send_email()`方法中优先读取环境变量：

```python
import os

def send_wechat(self, content, webhook_url=None):
    if webhook_url is None:
        webhook_url = os.environ.get('WECHAT_WEBHOOK') or \
                      self.config.get('wechat', {}).get('webhook_url')
    ...

def send_email(self, subject, body, to_email=None):
    sender_password = os.environ.get('EMAIL_PASSWORD') or \
                      self.config.get('email', {}).get('sender_password')
    ...
```

**修改文件**：`scripts/config.yaml`

在通知配置部分添加安全提示注释：

```yaml
# 微信企业机器人配置
wechat:
  webhook_url: ""  # 建议使用环境变量 WECHAT_WEBHOOK

# 邮件配置
email:
  sender_password: ""  # 建议使用环境变量 EMAIL_PASSWORD
```

#### 步骤3：请求缓存实现

**修改文件**：`scripts/fund_data_fetcher_v2.py`

在模块顶部添加缓存初始化：

```python
import requests_cache

requests_cache.install_cache(
    'fund_api_cache',
    expire_after=3600,
    backend='sqlite'
)
```

这样所有requests请求自动缓存1小时，减少对东方财富等接口的频繁调用。

#### 步骤4：配置文件模板更新

**修改文件**：`scripts/config.yaml`

在文件顶部添加安全使用说明：

```yaml
# =====================================================
# 安全提示：
# - 微信webhook和邮箱密码等敏感信息请使用环境变量
# - Windows: set WECHAT_WEBHOOK=your_webhook_url
# - Linux/Mac: export WECHAT_WEBHOOK=your_webhook_url
# =====================================================
```

---

### 第二阶段：数据质量与性能（优先级：高）

#### 步骤1：数据完整性校验

**修改文件**：`scripts/fund_data_fetcher_v2.py`

在FundDataFetcherV2类中添加数据校验方法：

```python
def _validate_nav_data(self, df):
    if df.empty:
        return False
    latest_date = df.index[-1]
    if (pd.Timestamp.now() - latest_date).days > 5:
        return False
    if (df['单位净值'] <= 0).any():
        return False
    return True
```

在`get_fund_nav()`方法中，每个数据源返回结果后调用校验：

```python
result = self._get_from_source(source, fund_code, start_date, end_date)
if result is not None and not result.empty and self._validate_nav_data(result):
    return result
```

#### 步骤2：数据库索引优化

**修改文件**：`scripts/fund_database.py`

在`init_database()`方法的索引创建部分，添加复合索引：

```python
cursor.execute('CREATE INDEX IF NOT EXISTS idx_fund_nav_code_date ON fund_nav(fund_code, nav_date)')
cursor.execute('CREATE INDEX IF NOT EXISTS idx_fund_analysis_code_date ON fund_analysis(fund_code, analysis_date)')
```

#### 步骤3：数据库清理功能

**修改文件**：`scripts/fund_database.py`

添加清理旧数据方法：

```python
def cleanup_old_data(self, retention_days=1095):
    cursor = self.conn.cursor()
    cutoff_date = (datetime.now() - timedelta(days=retention_days)).strftime('%Y-%m-%d')
    cursor.execute("DELETE FROM fund_nav WHERE nav_date < ?", (cutoff_date,))
    cursor.execute("DELETE FROM fund_analysis WHERE analysis_date < ?", (cutoff_date,))
    self.conn.commit()
    print(f"已清理 {cutoff_date} 之前的数据")
```

在config.yaml中添加清理配置：

```yaml
database:
  path: "fund_data.db"
  backup_enabled: true
  backup_retention_days: 30
  data_retention_days: 1095  # 数据保留天数（3年）
```

---

### 第三阶段：投资分析增强（优先级：高）

#### 步骤1：分析报告添加免责声明

**修改文件**：`scripts/fund_analyzer_v2.py`

在`generate_report()`方法末尾添加：

```python
report += f"""
七、免责声明
- 技术指标仅供学习参考，不可用于高频择时
- 基金净值不是连续交易价格，RSI、MACD等指标会滞后
- 历史数据不代表未来表现，投资需谨慎
"""
```

#### 步骤2：回测加入交易成本和滑点

**修改文件**：`scripts/main_cli.py`

修改`_calculate_backtest_stats()`方法，添加交易成本参数：

```python
def _calculate_backtest_stats(self, signals, nav_series, commission=0.0015, slippage=0.001):
    initial_capital = self.config.get('backtest.initial_capital', 10000)
    shares = 0
    capital = initial_capital
    trades = []
    total_cost = 0

    for signal_type, date, price in signals:
        if signal_type == '买入':
            actual_price = price * (1 + slippage)
            cost = capital * commission
            shares = (capital - cost) / actual_price
            total_cost += cost
            capital = 0
        elif signal_type == '卖出':
            actual_price = price * (1 - slippage)
            capital = shares * actual_price
            cost = capital * commission
            capital -= cost
            total_cost += cost
            shares = 0

        trades.append({
            'type': signal_type, 'date': date, 'price': price,
            'capital': capital, 'shares': shares
        })

    final_value = capital + shares * nav_series.iloc[-1]
    total_return = (final_value - initial_capital) / initial_capital * 100

    return {
        '初始资金': initial_capital,
        '最终价值': round(final_value, 2),
        '总收益率': round(total_return, 2),
        '总交易成本': round(total_cost, 2),
        '交易次数': len(signals) // 2,
        '交易记录': trades
    }
```

在config.yaml中添加回测成本配置：

```yaml
backtest:
  initial_capital: 10000
  short_ma: 5
  long_ma: 20
  commission: 0.0015    # 交易佣金（0.15%）
  slippage: 0.001       # 滑点（0.1%）
```

在回测结果输出中增加交易成本显示：

```python
print(f"  总交易成本: {stats.get('总交易成本', 0):.2f} 元")
```

#### 步骤3：持有期惩罚功能

**修改文件**：`scripts/fund_analyzer_v2.py`

添加带持有期惩罚的定投模拟方法：

```python
def simulate_dca_with_penalty(self, fund_data, monthly_amount=500,
                               min_hold_days=7, penalty_rate=0.015):
    if fund_data.empty:
        return {}
    monthly_data = fund_data.resample('M').first()
    total_shares = 0
    total_invested = 0
    total_penalty = 0
    last_buy_date = None
    invest_history = []

    for date, row in monthly_data.iterrows():
        nav = row['单位净值']
        shares = monthly_amount / nav
        total_shares += shares
        total_invested += monthly_amount
        last_buy_date = date

        invest_history.append({
            '日期': date, '净值': nav, '份额': shares,
            '累计份额': total_shares, '累计投入': total_invested,
            '当前市值': total_shares * nav
        })

    final_nav = fund_data['单位净值'].iloc[-1]
    final_value = total_shares * final_nav
    total_return = (final_value - total_invested) / total_invested * 100

    return {
        '累计投入': round(total_invested, 2),
        '累计份额': round(total_shares, 4),
        '最终市值': round(final_value, 2),
        '总收益率': round(total_return, 4),
        '投资月数': len(monthly_data),
        '持有期惩罚': round(total_penalty, 2),
        '定投历史': invest_history
    }
```

#### 步骤4：基准对比功能

**修改文件**：`scripts/fund_analyzer_v2.py`

添加基准对比方法：

```python
def compare_with_benchmark(self, fund_data, benchmark_data, fund_code="", benchmark_code="000300"):
    fund_returns = self.calculate_returns(fund_data['单位净值'])
    benchmark_returns = self.calculate_returns(benchmark_data['单位净值'])

    excess_return = fund_returns.get('总收益率', 0) - benchmark_returns.get('总收益率', 0)

    fund_daily = fund_data['单位净值'].pct_change().dropna()
    bench_daily = benchmark_data['单位净值'].pct_change().dropna()
    aligned = pd.concat([fund_daily, bench_daily], axis=1).dropna()

    if len(aligned) > 1:
        tracking_error = (aligned.iloc[:, 0] - aligned.iloc[:, 1]).std() * np.sqrt(252) * 100
        information_ratio = excess_return / tracking_error if tracking_error > 0 else 0
    else:
        tracking_error = 0
        information_ratio = 0

    return {
        '基金代码': fund_code,
        '基准代码': benchmark_code,
        '基金收益率': round(fund_returns.get('总收益率', 0), 2),
        '基准收益率': round(benchmark_returns.get('总收益率', 0), 2),
        '超额收益': round(excess_return, 2),
        '跟踪误差': round(tracking_error, 2),
        '信息比率': round(information_ratio, 4),
        '跑赢基准': excess_return > 0
    }
```

**修改文件**：`scripts/main_cli.py`

添加compare操作：

```python
def compare(self, fund_code, benchmark_code='110030', days=365):
    start_date = (datetime.now() - timedelta(days=days)).strftime('%Y-%m-%d')
    end_date = datetime.now().strftime('%Y-%m-%d')

    fund_data = self.fetcher.get_fund_nav(fund_code, start_date, end_date)
    benchmark_data = self.fetcher.get_fund_nav(benchmark_code, start_date, end_date)

    if fund_data.empty or benchmark_data.empty:
        print("无法获取数据")
        return

    result = self.analyzer.compare_with_benchmark(fund_data, benchmark_data, fund_code, benchmark_code)

    print(f"\n基金 vs 基准对比")
    print(f"  基金收益率: {result['基金收益率']}%")
    print(f"  基准收益率: {result['基准收益率']}%")
    print(f"  超额收益: {result['超额收益']}%")
    print(f"  跟踪误差: {result['跟踪误差']}%")
    print(f"  信息比率: {result['信息比率']}")
    print(f"  {'跑赢基准' if result['跑赢基准'] else '跑输基准'}")
```

---

### 第四阶段：用户体验提升（优先级：中）

#### 步骤1：交互式向导模式

**修改文件**：`scripts/main_cli.py`

添加`--wizard`参数和向导方法：

```python
def run_wizard(self):
    print("\n" + "="*50)
    print("基金投资分析向导")
    print("="*50)

    fund_code = input("\n请输入基金代码（如110011）: ").strip()
    monthly_amount = input("每月定投金额（元，默认500）: ").strip() or "500"
    threshold = input("日跌幅预警阈值（%，默认-3）: ").strip() or "-3"

    print("\n通知方式：1.控制台 2.微信 3.邮件")
    method = input("请选择（1-3，默认1）: ").strip() or "1"
    method_map = {'1': 'console', '2': 'wechat', '3': 'email'}

    self.config.add_fund(fund_code, '', int(monthly_amount))
    self.config.set('alert.daily_drop_threshold', float(threshold))
    self.config.set('notification.method', method_map.get(method, 'console'))
    self.config.save_config()

    print(f"\n配置完成！运行以下命令开始分析：")
    print(f"python main_cli.py --action analyze --fund {fund_code}")
```

在argparse中添加：

```python
parser.add_argument('--wizard', action='store_true', help='交互式向导模式')
```

#### 步骤2：投资日志模块

**修改文件**：`scripts/fund_database.py`

添加journal表创建（在`init_database()`中）：

```python
cursor.execute('''
CREATE TABLE IF NOT EXISTS journal (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT,
    fund_code TEXT,
    action TEXT,
    amount REAL,
    nav REAL,
    reason TEXT,
    emotion TEXT,
    reflection TEXT,
    update_time TEXT
)
''')
```

添加日志操作方法：

```python
def add_journal_entry(self, fund_code, action, amount, nav, reason, emotion, reflection=""):
    cursor = self.conn.cursor()
    cursor.execute('''
    INSERT INTO journal (date, fund_code, action, amount, nav, reason, emotion, reflection, update_time)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    ''', (
        datetime.now().strftime('%Y-%m-%d %H:%M:%S'),
        fund_code, action, amount, nav, reason, emotion, reflection,
        datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    ))
    self.conn.commit()

def get_journal_entries(self, fund_code=None, limit=20):
    cursor = self.conn.cursor()
    if fund_code:
        cursor.execute("SELECT * FROM journal WHERE fund_code=? ORDER BY date DESC LIMIT ?",
                       (fund_code, limit))
    else:
        cursor.execute("SELECT * FROM journal ORDER BY date DESC LIMIT ?", (limit,))
    columns = [desc[0] for desc in cursor.description]
    return [dict(zip(columns, row)) for row in cursor.fetchall()]

def review_month(self, year, month):
    cursor = self.conn.cursor()
    start_date = f"{year}-{month:02d}-01"
    end_date = f"{year}-{month:02d}-31"
    cursor.execute('''
    SELECT action, COUNT(*) as count, SUM(amount) as total_amount
    FROM journal WHERE date BETWEEN ? AND ? GROUP BY action
    ''', (start_date, end_date))
    columns = [desc[0] for desc in cursor.description]
    return [dict(zip(columns, row)) for row in cursor.fetchall()]
```

**修改文件**：`scripts/main_cli.py`

添加journal和review命令行操作。

#### 步骤3：交互式HTML图表

**修改文件**：`scripts/fund_analyzer_v2.py`

添加plotly交互式图表方法：

```python
def create_interactive_chart(self, fund_data, fund_code, save_path=None):
    import plotly.graph_objects as go
    from plotly.subplots import make_subplots

    nav_series = fund_data['单位净值']

    fig = make_subplots(rows=3, cols=1, shared_xaxes=True,
                        subplot_titles=('净值走势', '累计收益率', '回撤分析'),
                        vertical_spacing=0.08)

    fig.add_trace(go.Scatter(x=nav_series.index, y=nav_series.values,
                             mode='lines', name='单位净值'), row=1, col=1)

    if len(nav_series) >= 20:
        ma20 = nav_series.rolling(20).mean()
        fig.add_trace(go.Scatter(x=ma20.index, y=ma20.values,
                                 mode='lines', name='MA20',
                                 line=dict(dash='dash')), row=1, col=1)

    daily_returns = nav_series.pct_change().dropna()
    cumulative = (1 + daily_returns).cumprod() - 1
    fig.add_trace(go.Scatter(x=cumulative.index, y=cumulative * 100,
                             mode='lines', name='累计收益率'), row=2, col=1)

    peak = nav_series.expanding(min_periods=1).max()
    drawdown = (nav_series - peak) / peak * 100
    fig.add_trace(go.Scatter(x=drawdown.index, y=drawdown,
                             fill='tozeroy', name='回撤'), row=3, col=1)

    fig.update_layout(title=f'基金 {fund_code} 交互式分析',
                      height=800, showlegend=True)

    if save_path is None:
        save_path = f"fund_{fund_code}_interactive.html"
    fig.write_html(save_path)
    print(f"交互式图表已保存: {save_path}")
```

#### 步骤4：增强帮助信息

**修改文件**：`scripts/main_cli.py`

更新argparse epilog，包含所有新功能的使用示例。

---

### 第五阶段：智能定投与扩展（优先级：中）

#### 步骤1：智能定投（价值平均策略）

**修改文件**：`scripts/fund_analyzer_v2.py`

添加价值平均定投方法：

```python
def simulate_value_averaging(self, fund_data, target_growth=500, initial_value=500):
    if fund_data.empty:
        return {}

    monthly_data = fund_data.resample('M').first()
    total_shares = initial_value / monthly_data['单位净值'].iloc[0]
    total_invested = initial_value
    invest_history = []

    for i, (date, row) in enumerate(monthly_data.iterrows()):
        if i == 0:
            invest_history.append({
                '日期': date, '净值': row['单位净值'],
                '目标市值': initial_value, '当前市值': initial_value,
                '投入金额': initial_value, '累计投入': total_invested
            })
            continue

        nav = row['单位净值']
        target_value = initial_value + target_growth * i
        current_value = total_shares * nav
        invest_amount = target_value - current_value

        if invest_amount > 0:
            new_shares = invest_amount / nav
            total_shares += new_shares
            total_invested += invest_amount
        elif invest_amount < 0:
            sell_amount = min(abs(invest_amount), current_value * 0.2)
            sell_shares = sell_amount / nav
            total_shares -= sell_shares
            total_invested -= sell_amount

        invest_history.append({
            '日期': date, '净值': nav,
            '目标市值': round(target_value, 2),
            '当前市值': round(total_shares * nav, 2),
            '投入金额': round(invest_amount, 2),
            '累计投入': round(total_invested, 2)
        })

    final_value = total_shares * monthly_data['单位净值'].iloc[-1]
    total_return = (final_value - total_invested) / total_invested * 100

    return {
        '策略': '价值平均定投',
        '目标增长': f'{target_growth}元/月',
        '累计投入': round(total_invested, 2),
        '最终市值': round(final_value, 2),
        '总收益率': round(total_return, 2),
        '投资月数': len(monthly_data),
        '投资历史': invest_history
    }
```

**修改文件**：`scripts/main_cli.py`

添加`--strategy`参数支持dca/value_avg策略选择：

```python
parser.add_argument('--strategy', choices=['dca', 'value_avg'],
                    default='dca', help='定投策略（dca=普通定投，value_avg=价值平均）')
```

#### 步骤2：--danger模式

**修改文件**：`scripts/main_cli.py`

在main()函数中添加危险操作检查：

```python
DANGER_MESSAGE = """
{'!'*60}
WARNING: 你正在试图预测市场！
{'!'*60}

历史数据显示，超过80%的散户频繁择时会导致收益低于买入持有。

建议你先运行：
  python main_cli.py --action simulate --fund CODE

对比一下定投与一次性投入的收益差异。

{'!'*60}
"""
```

#### 步骤3：配置文件热加载

**修改文件**：`scripts/config_manager.py`

添加reload方法和watchdog支持（可选依赖）：

```python
def reload(self):
    self.config = self.DEFAULT_CONFIG.copy()
    self.load_config()
    print("配置已重新加载")
```

添加`--reload`参数支持（监控模式下自动重载配置）。

---

### 第六阶段：报表导出与文档（优先级：中）

#### 步骤1：HTML报表导出

**新建文件**：`scripts/fund_report.py`

使用jinja2生成HTML分析报表：

```python
from jinja2 import Template
from datetime import datetime

class FundReporter:
    def __init__(self, output_dir='.'):
        self.output_dir = output_dir

    def generate_html_report(self, fund_code, analysis, indicators=None):
        template = Template(HTML_TEMPLATE)
        html = template.render(
            fund_code=fund_code,
            generate_time=datetime.now().strftime('%Y-%m-%d %H:%M:%S'),
            analysis=analysis,
            indicators=indicators or {}
        )
        output_path = f"{self.output_dir}/fund_{fund_code}_report.html"
        with open(output_path, 'w', encoding='utf-8') as f:
            f.write(html)
        print(f"HTML报表已生成: {output_path}")
        return output_path
```

**修改文件**：`scripts/main_cli.py`

添加report操作：

```python
parser.add_argument('--action', choices=['analyze', 'monitor', 'backtest',
    'simulate', 'list', 'config', 'compare', 'journal', 'review', 'report'])
```

#### 步骤2：更新README.md

**修改文件**：`scripts/README.md`

添加：
- 免责声明
- 新增依赖安装说明
- 新功能使用示例（wizard、journal、compare、report）
- 智能定投说明
- 环境变量配置说明

#### 步骤3：更新配置文件模板

**修改文件**：`scripts/config.yaml`

添加：
- 安全提示注释
- 回测成本配置
- 数据保留天数配置

---

## 三、修改文件汇总

| 阶段 | 修改文件 | 改动类型 |
|------|----------|----------|
| 1 | config_manager.py | 添加get_secure()和SECURE_CONFIG_MAP |
| 1 | fund_notifier.py | 凭证读取改为环境变量优先 |
| 1 | config.yaml | 添加安全提示注释 |
| 1 | fund_data_fetcher_v2.py | 添加请求缓存 |
| 1 | 所有脚本文件 | 顶部添加免责声明docstring |
| 1 | README.md | 顶部添加免责声明 |
| 2 | fund_data_fetcher_v2.py | 添加_validate_nav_data() |
| 2 | fund_database.py | 添加复合索引和cleanup_old_data() |
| 3 | fund_analyzer_v2.py | 添加免责声明到报告、compare_with_benchmark() |
| 3 | main_cli.py | 回测添加交易成本、添加compare操作 |
| 3 | config.yaml | 添加回测成本配置 |
| 4 | main_cli.py | 添加wizard/journal/review操作 |
| 4 | fund_database.py | 添加journal表和相关方法 |
| 4 | fund_analyzer_v2.py | 添加create_interactive_chart() |
| 5 | fund_analyzer_v2.py | 添加simulate_value_averaging() |
| 5 | main_cli.py | 添加--strategy参数、danger检查 |
| 5 | config_manager.py | 添加reload()方法 |
| 6 | fund_report.py（新建） | HTML报表生成 |
| 6 | main_cli.py | 添加report操作 |
| 6 | README.md | 全面更新文档 |

---

## 四、实施时间表

| 阶段 | 内容 | 优先级 | 预计改动文件数 |
|------|------|--------|----------------|
| 第一阶段 | 安全与合规 | 高 | ~12个 |
| 第二阶段 | 数据质量与性能 | 高 | 2个 |
| 第三阶段 | 投资分析增强 | 高 | 3个 |
| 第四阶段 | 用户体验提升 | 中 | 3个 |
| 第五阶段 | 智能定投与扩展 | 中 | 3个 |
| 第六阶段 | 报表导出与文档 | 中 | 3个（含1个新建） |

---

## 五、预期成果

### 功能增强清单
- 免责声明覆盖所有脚本和README
- 凭证通过环境变量安全存储
- HTTP请求自动缓存（1小时过期）
- 数据完整性校验（心跳检测）
- 数据库复合索引和自动清理
- 回测含交易成本（0.15%佣金 + 0.1%滑点）
- 基金 vs 沪深300基准对比
- 投资日志记录和月度复盘
- 交互式向导模式
- 智能定投（价值平均策略）
- 交互式HTML图表（plotly）
- HTML报表导出（jinja2）
- 配置文件热加载（watchdog）
- --danger危险操作警告

### 用户体验提升
- 新手可通过`--wizard`一键配置
- 投资日志帮助用户复盘成长
- 交互式图表比PNG更直观
- HTML报表便于分享

### 安全合规保障
- 敏感凭证不硬编码
- 所有SQL参数化（已满足）
- 请求频率受缓存控制
- 免责声明覆盖全面

---

**计划制定时间**：2026年5月24日
**基于文档**：优化2.md
**当前状态**：计划制定完成，等待批准执行
